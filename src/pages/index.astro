---
import { getCollection } from 'astro:content';

const posts = await getCollection('posts');
---

<h1>Preguntas de Entrevista por Puesto</h1>

<ul>
  {posts.map(post => (
    <li>
      <a href={`/posts/${post.slug}/`}>{post.data.title}</a>
    </li>
  ))}
</ul>
